<%= simple_form_for(@election) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :years_range %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
