<div class="pull-right"><%= link_to 'New Complaint', new_complaint_path, class: 'btn btn-primary' %></div>

<div class="well well-small">
  <%= form_for_filterrific @filterrific do |f| %>
    <div>
      Search:
      <%= f.text_field(:search_query, class: 'filterrific-periodically-observed') %>
    </div>

    <div>
      Registered after:
      <%= f.text_field(:with_created_at_gte, class: 'js-datepicker') %>
    </div><br/>

    <%= link_to('Reset filters', reset_filterrific_url) %>
    <%= link_to('Public', complaints_path(public_data: true), class: 'btn btn-primary') %>
    <%= link_to('Own', complaints_path, class: 'btn btn-primary') %>

    <%= render_filterrific_spinner %>
  <% end %>
</div>


  <h1>Listing Complaints</h1>
  <div id="filterrific_results"><%= render partial: 'complaints/list', locals: { complaints: @complaints } %></div>

<br>

<%= link_to 'New Complaint', new_complaint_path %>
