$('.posts-index').append("<%= j render partial:'post', collection: @posts, as: :post %>");
<% if @posts.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @posts %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
