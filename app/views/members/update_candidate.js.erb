<% js = escape_javascript(
  render(partial: 'members/partials/select_candidate', locals: { member: @member})
) %>
$("#mark-as-pres-<%=@member.id%>").html("<%= js %>");

toastr.success('Success');

location.reload();
