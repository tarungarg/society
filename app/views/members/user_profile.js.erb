$('.posts-index').append("<%= j render partial:'posts/post', collection: @posts, as: :post %>");
<% if @posts.next_page %>
  $('#infinite-scrolling .pagination').replaceWith('<%= j will_paginate @posts %>');
<% else %>
  $(window).off('scroll');
  $('#infinite-scrolling .pagination').remove();
<% end %>
